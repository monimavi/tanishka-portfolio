import Reveal from './Reveal';
const steps=[['01','Understand','Requirements, goals and current workflow.'],['02','Plan','Architecture, data flow and delivery scope.'],['03','Build','Frontend, backend, database and integrations.'],['04','Test','Responsive QA, forms and workflow testing.'],['05','Deploy','Production launch, hosting and ongoing support.']];
export default function Process(){return <section className="process-section section-pad"><div className="shell process-head"><div><div className="section-kicker"><span/> My Process</div><h2>From Requirement<br/>to Production</h2></div><p>A practical workflow that keeps the build clear, testable and production-focused.</p></div><div className="shell process-row">{steps.map((s,i)=><Reveal key={s[0]} delay={i*70}><div className="process-card"><span>{s[0]}</span><h3>{s[1]}</h3><p>{s[2]}</p></div></Reveal>)}</div></section>}
